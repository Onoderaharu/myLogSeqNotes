- 并发编程的目标，是为了让程序运行的更快，但并不是启动的线程越多，运行的越快，会面临很多问题：上下文切换、死锁、受限于硬件和软件的资源问题。
- # 第一章
	- ## 1.上线文切换
	- CPU通过给每个线程分配时间片，实现多线程。执行完当前时间片后，切换到下个时间片之前，会保存上一个任务的状态，以便下次切换到这个任务时，可以加载这个任务的状态。任务从保存到再加载的过程，就是一次上下文切换。
	- 上下问的切换，会影响多线程执行速度。
	- ## 2.如何减少上线文的切换
	- （1）无锁并发编程。多线程竞争锁时，会引起上线文切换，多以线程处理数据时，可以用一些办法避免使用锁
	- （2）CAS算法。？？？
	  id:: 634d0066-9bd9-46b6-b0e4-6ab9cce8b981
	  background-color:: #793e3e
	- （3）使用最少线程。任务很少但是使用了大量线程，会造成大量线程处于等待状态
	- （4）协程。？？？
	  background-color:: #793e3e
	-
	-
	- ## 3.资源限制
	- 硬件资源限制：带宽的上传/下载速度、硬盘读写速度、CPU的处理速度
	- 软件资源限制：数据库的连接数、socket连接数等
	- ## 4.如何解决资源限制
	- 硬件：集群
	- 软件：使用资源池将资源复用；调用对方webservice接口获取数据时，只建立一个连接